# GitHub Packages registry for @ezds/* scope.
# The auth token is injected by CI (actions/setup-node) or by consumers via NODE_AUTH_TOKEN.
# Local development does not need this file — workspace symlinks are used instead.
@ezds:registry=https://npm.pkg.github.com
