import { Meta } from "@storybook/blocks";
import { ComponentRules, UsageGuidelines } from "vibe-storybook-components";
import * as AttentionBoxStories from "./AttentionBox.stories";
import AttentionBox from "../AttentionBox";

<Meta of={AttentionBoxStories} />

# AttentionBox

The AttentionBox component communicates important, contextually relevant information within content areas. It alerts, informs, or guides users about crucial actions, warnings, or system states inline with user workflows.

## Import path

```tsx
import { AttentionBox } from "@vibe/core";
```

## Overview

<Canvas of={AttentionBoxStories.Overview} />

## Props

<PropsTable />

## Usage

<UsageGuidelines
  guidelines={[
    "Use AttentionBox to surface inline alerts and important messages within workflow contexts.",
    "Choose the appropriate variant (primary, success, danger, warning, dark) to match the message semantics.",
    "Enable compact mode for space-constrained layouts, and enable multiline only when wrapping is needed.",
    "Provide a clear title in default mode and include actionable elements like links or buttons for follow-up."
  ]}
/>

## Variants

### Success Variant

Illustrates a successful operation state.

<Canvas of={AttentionBoxStories.SuccessVariant} />

### Danger Variant

Illustrates error or destructive information.

<Canvas of={AttentionBoxStories.DangerVariant} />

### Warning Variant

Indicates cautionary messages.

<Canvas of={AttentionBoxStories.WarningVariant} />

### Dark Variant

Displays a dark-themed attention box for neutral or custom contexts.

<Canvas of={AttentionBoxStories.DarkVariant} />

### Compact Mode

Displays a compact, single-line attention box.

<Canvas of={AttentionBoxStories.CompactSingleLine} />

### Compact Multiline Mode

Allows the compact attention box to wrap to multiple lines.

<Canvas of={AttentionBoxStories.CompactMultiline} />

### Action and Link

Shows how to include an inline action button and link.

<Canvas of={AttentionBoxStories.WithActionAndLink} />

### Dismissible Example

Demonstrates how the attention box can be dismissed and re-displayed by the parent component.

<Canvas of={AttentionBoxStories.Dismissible} />

### No Animation

Shows the attention box rendered without entry animation.

<Canvas of={AttentionBoxStories.NoAnimation} />

## Do's and Don'ts

<ComponentRules
  rules={[
    {
      positive: {
        component: <></>,
        description:
          "Use AttentionBox to draw users' attention to important inline messages without disrupting the main content flow."
      },
      negative: {
        component: <></>,
        description: "Avoid using AttentionBox for trivial information that does not require immediate attention."
      }
    },
    {
      positive: {
        component: <></>,
        description: "Include a clear, concise title in default mode to immediately convey the message context."
      },
      negative: {
        component: <></>,
        description: "Don't omit the title in default mode or use overly technical language in titles."
      }
    },
    {
      positive: {
        component: <></>,
        description:
          "Use compact mode in space-constrained areas, and enable multiline only when necessary to prevent truncation."
      },
      negative: {
        component: <></>,
        description: "Don't force single-line compact mode for long messages that require full readability."
      }
    },
    {
      positive: {
        component: <></>,
        description: "Provide actionable elements like links or buttons for direct follow-up within the AttentionBox."
      },
      negative: {
        component: <></>,
        description: "Don't place unrelated or multiple conflicting actions inside the same AttentionBox."
      }
    }
  ]}
/>
