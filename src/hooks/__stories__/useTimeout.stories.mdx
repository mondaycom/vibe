import { useRef, useCallback } from "react"
import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import useTimeout from "../useTimeout";
import UseTimeout from "../../hooksDummyComponents/UseTimeout";

<Meta
  title="Hooks/useTimeout"
  component={ UseTimeout }
/>

# useTimeout
- [Overview](#overview)
- [Props](#props)
- [Feedback](#feedback)

## Overview
Please write here the component description

<Canvas>
  <Story name="Overview">
    {() => {
      const ref = useRef(null);
      const callback = useCallback(() => {
        alert("5s passed")
      })
      useTimeout({ time: 5000, callback });
      return (
        <div ref={ref}>
          Reference Element
        </div>
      );
    }}
  </Story>
</Canvas>

## Props
<ArgsTable of={ UseTimeout } />

