import Loader from "../Loader";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { createComponentTemplate } from "../../../storybook/functions/create-component-story";
import { StoryDescription } from "../../../storybook/components/story-description/story-description";
import { Flex } from "components";

<Meta title="Feedback/Loader" component={Loader} />

<!--- Component template -->

export const loaderTemplate = createComponentTemplate(Loader);

<!--- Component documentation -->

# Loader

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)
- [Feedback](#feedback)

## Overview

Circle loading indication, a general non-specific loader.

<Canvas>
  <Story name="Overview" args={{ size: 40 }}>
    {loaderTemplate.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable of={Loader} />

## Usage

<UsageGuidelines
  guidelines={[
    "Use loader component to indicate the user that a task is being performed",
    "If you expect that a task will be done within 200ms don't show a loader",
    "Minimize the loaders on the screen"
  ]}
/>

## Variants

### Themes

There are three color constant types provided.

<Canvas>
  <Story name="Themes" >
    <Flex direction={Flex.directions.COLUMN} gap={Flex.gaps.MEDIUM}>
      <StoryDescription description="Primary">
        <Loader size={40} color={Loader.colors.PRIMARY}/>
      </StoryDescription>
      <StoryDescription description="OnPrimary">
        <Loader size={40} color={Loader.colors.ON_PRIMARY}/>
      </StoryDescription>
      <StoryDescription description="Dark">
        <Loader size={40} color={Loader.colors.DARK}/>
      </StoryDescription>
    </Flex>
  </Story>
</Canvas>

### Custom theme

If there is a need for color customization, css `color` attribute of a parent component can be used.

<Canvas>
  <Story name="Color inheritance">
    <div style={{ color: "var(--color-dark-red)" }}>
      <Loader size={40} />
    </div>
  </Story>
</Canvas>