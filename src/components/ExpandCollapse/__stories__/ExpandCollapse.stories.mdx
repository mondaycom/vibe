import ExpandCollapse from "../ExpandCollapse";
import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { createStoryMetaSettingsDecorator, RelatedComponentsDecorator } from "../../../storybook";
import { ExpandCollapseCustomHeadingComponent, expandCollapseTemplate } from "./expandCollapse.stories";
import { Link } from "vibe-storybook-components";
import {
  ACCORDION,
  DROPDOWN,
  WIZARD
} from "../../../storybook/components/related-components/component-description-map";

export const metaSettings = createStoryMetaSettingsDecorator({
  component: ExpandCollapse,
  enumPropNamesArray: [], // List enum props here
  iconPropNamesArray: [], // List props that are typed as icons here
  actionPropsArray: [] // List the component's actions here
});

<Meta
  title="Data Display/ExpandCollapse"
  component={ExpandCollapse}
  argTypes={metaSettings.argTypes}
  decorators={metaSettings.decorators}
/>

# ExpandCollapse

- [Overview](#overview)
- [Props](#props)
- [Variants](#variants)
- [Related components](#related-components)
- [Feedback](#feedback)

## Overview

ExpandCollapse is a component that allows you to hide and show content.

<Canvas>
  <Story name="Overview" args={{ title: "Expand collapse" }}>
    {expandCollapseTemplate.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

<Tip title="Want to combine multiple ExpandCollapse?">
  Use our
  <Link size={Link.sizes.SMALL} href="/?path=/docs/data-display-accordion--overview">
    Accordion
  </Link>
  component
</Tip>

## Variants

### Open by default

<Canvas>
  <Story name="Open by default" args={{ defaultOpenState: true, title: "ExpandCollapse title" }}>
    {expandCollapseTemplate.bind({})}
  </Story>
</Canvas>

### Custom header renderer

<Canvas>
  <Story
    name="Custom header renderer"
    args={{
      headerComponentRenderer: ExpandCollapseCustomHeadingComponent
    }}
  >
    {expandCollapseTemplate.bind({})}
  </Story>
</Canvas>

### Without borders

<Canvas>
  <Story
    name="Without borders"
    args={{
      title: "ExpandCollapse title",
      hideBorder: true
    }}
  >
    {expandCollapseTemplate.bind({})}
  </Story>
</Canvas>

## Related components

<RelatedComponentsDecorator componentsNames={[ACCORDION, DROPDOWN, WIZARD]} />
