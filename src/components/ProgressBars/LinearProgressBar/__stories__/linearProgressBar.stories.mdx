import LinearProgressBar from "../LinearProgressBar";
import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { createComponentTemplate } from "../../../../storybook/functions/create-component-story";
import colors from "../../../../constants/colors.json";
import "./linearProgressBar.stories.scss";

<Meta
  title="Components/LinearProgressBar"
  component={ LinearProgressBar }
/>
<!--- Component template -->

export const lineProgressBarTemplate = createComponentTemplate(LinearProgressBar);

<!--- Component documentation -->

# LinearProgressBar
- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)
- [Do’s and don’ts](#dos-and-donts)
- [Use cases and examples](#use-cases-and-examples)
- [Related components](#related-components)
- [Feedback](#feedback)

## Overview
Accordion is a vertically stacked list of items. Each item can be "expanded" or "collapsed" to reveal the content within with that item.

<Canvas>
  <Story name="Overview"
         args={ {prop1: "prop's default value", prop2: "prop's default value"} }>
    { lineProgressBarTemplate.bind({}) }
  </Story>
</Canvas>

## Props
<ArgsTable of={ LinearProgressBar } />

## Usage
<UsageGuidelines guidelines={[
  "Give users an indication of how much of the task has completed and how much is left."
]}/>

<Tip>If you need to lead a user through a progress, use the Stepper instead.</Tip>

## Variants
### Default
<Canvas>
  <Story name="Default">
    <div className="a">
      <LinearProgressBar value={20} size={LinearProgressBar.sizes.LARGE} />
    </div>
  </Story>
</Canvas>

### Multi progress bar
<Canvas>
  <Story name="Multi progress bar">
    {() => {
      const multiValues = [
        { value: 25, color: "red" },
        { value: 75, color: "yellow" },
        { value: 100, color: "green" }
      ];
      return (
        <LinearProgressBar 
          value={25}
          size={LinearProgressBar.sizes.LARGE}
          indicateProgress
          multi
          multiValues={multiValues}
        />
      )
    }}
  </Story>
</Canvas>

## Do’s and Don’ts
<ComponentRules
  rules={[
    {
      positive: {
        component:[],
        description:"Use a progress bar only process has start and finish point."
      },
      negative: {
        component:[],
        description:"Don’t use an infinite scalable indicator."
      }
    },
    {
      positive: {
        component:[],
        description:"Always indincate visually the empty part of the progress bar."
      },
      negative: {
        component:[],
        description:"Don’t let the user guess where is the end of the progress."
      }
    }
  ]}
/>