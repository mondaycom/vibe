import EditableHeading from "../EditableHeading";
import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { createStoryMetaSettingsDecorator } from "../../../storybook";
import { createComponentTemplate } from "vibe-storybook-components";
import { useState } from "react";
import { overviewPlaySuite } from "../__tests__/EditableHeading-interactions";
import {
  TEXT,
  HEADING,
  EDITABLE_TEXT
} from "../../../storybook/components/related-components/component-description-map";

export const metaSettings = createStoryMetaSettingsDecorator({
  component: EditableHeading,
  enumPropNamesArray: ["weight", "type"],
  actionPropsArray: ["onChange"]
});

<Meta
  title="Inputs/EditableHeading"
  component={EditableHeading}
  argTypes={metaSettings.argTypes}
  decorators={metaSettings.decorators}
/>

export const editableHeadingTemplate = createComponentTemplate(EditableHeading);

# EditableHeading

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)
- [Related components](#related-components)
- [Feedback](#feedback)

## Overview

Editable Heading component is based on the <Link href="/?path=/docs/text-heading--overview" withoutSpacing>Heading</Link> component, allowing users to dynamically edit its content.

<Canvas>
  <Story
    name="Overview"
    args={{
      value: "This heading is an editable heading",
      type: EditableHeading.types.H1
    }}
    play={overviewPlaySuite}
  >
    {editableHeadingTemplate.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Usage

<UsageGuidelines
  guidelines={[
    "Use when you want to allow the user to edit the heading text",
    "This component grows relative to its content, and its maximum width is 100% of its container "
  ]}
/>

## Variants

### Heading types

<Canvas>
  <Story name="Types">
    <EditableHeading type={EditableHeading.types.H1} value="H1 Header" />
    <EditableHeading type={EditableHeading.types.H2} value="H2 Header" />
    <EditableHeading type={EditableHeading.types.H3} value="H3 Header" />
  </Story>
</Canvas>

## Related components

<RelatedComponents componentsNames={[EDITABLE_TEXT, TEXT, HEADING]} />
