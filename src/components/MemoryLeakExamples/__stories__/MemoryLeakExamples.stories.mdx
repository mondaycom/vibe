import { GlobalLeak } from "../GlobalLeak";
import { DetachedTreeLeak } from "../DetachedTreeLeak";
import { createComponentTemplate, createStoryMetaSettings, } from "../../../storybook/";
import { Story, Canvas, Meta } from "@storybook/addon-docs";

export const metaSettings = createStoryMetaSettings({
  component: GlobalLeak
})

<Meta
  title="Buttons/MemoryLeakExamples"
  component={GlobalLeak}
  argTypes={ metaSettings.argTypes }
  decorators={ metaSettings.decorators }
/>

<!--- Component template -->

export const memoryLeakExampleTemplate = createComponentTemplate(GlobalLeak);

<!--- Component documentation -->

# Memory Leaks
## Overview
Examples of Memory Leak

## Variants

### Global Memory Leak
<Canvas>
  <Story name="GlobalMemoryLeak">
    <GlobalLeak />
  </Story>
</Canvas>

### Detached Tree Memory Leak
<Canvas>
  <Story name="DetachedTreeMemoryLeak">
    <DetachedTreeLeak />
  </Story>
</Canvas>
