import Button from "../Button";
import { Meta, Preview, ArgsTable, Story, Canvas } from "@storybook/addon-docs";
import Calendar from "../../Icon/Icons/components/Calendar";
import { ComponentRules } from "../../../storybook-components/component-rules/component-rules";

<Meta
  title="Components/Button"
  component={Button}
  argTypes={{
    kind: {
      description: "check"
    }
  }}
/>

# tbd: title
## Overview
Buttons allow users take actions with a single click.
They should be clear indications that they triggering an action, buttons should be easy to find among other UI.

<Preview>
  <Button>Button</Button>
</Preview>

## Props
<ArgsTable of={Button} />

## Usage
➡️  Buttons may contain icon, on the left or the right side

➡️  Use 8 px spacing between buttons.

➡️  Replace text with a loader if action is submitted, but still processing.

➡️  Button width is set by it’s content, avoid changing it width

➡️  Use only one primary button, and any remaining calls to action should be represented as lower emphasis buttons.


<Tip>Not sure what button size to use? Medium is our defualt size!</Tip>

## Variants
### Button types
There can be more than one button in a screen, but to create the hierarchy of actions we need to use button types
<Preview>
  <Button>Primary</Button>
  <Button kind={Button.kinds.SECONDARY}>Secondary</Button>
  <Button kind={Button.kinds.TERTIARY}>Tertiary</Button>
</Preview>


### Sizes
We have 3 button sizes to emphasise the action importance
<Canvas>
  <Story name="Sizes">
    <Button size={Button.sizes.LARGE}>Large</Button>
    <Button size={Button.sizes.MEDIUM}>Medium</Button>
    <Button size={Button.sizes.SMALL}>Small</Button>
  </Story>
</Canvas>

### States
<Canvas>
  <Story name="States">
    <Button>Regular</Button>
    <Button>Hover</Button>
    <Button>Active</Button>
    <Button disabled>Disabled</Button>
  </Story>
</Canvas>

### Error and success
<Canvas>
  <Story name="Error and success">
      <Button color={Button.colors.POSITIVE}>Positive</Button>
      <Button color={Button.colors.NEGATIVE}>Negative</Button>
  </Story>
</Canvas>

### Icons
<Canvas>
  <Story name="Icons">
    <Button rightIcon={Calendar}>Right icon</Button>
    <Button leftIcon={Calendar}>Left icon</Button>
  </Story>
</Canvas>

## Do’s and Don’ts
<ComponentRules
  rules={[
    {
      positive: {
        component:<Button>Get started</Button>,
        explanation:"Use 1 or 2 words, no longer than 4 words, with fewer than 20 characters including spaces."
      },
      negative: {
        component:<Button>Get started and enjoy discount!</Button>,
        explanation:"Don’t use punctuation marks such as periods or exclamation points. Also don’t wrap text."
      }
    }
  ]}
/>



## Use cases and examples
### Button with icon
Icons can be used in buttons when additional clarity is required and the icon is highly relevant to the action.
Icons should not be used for decoration. Don’t use two icons in the same button
tbd: example
### Loading state
In some cases, a button can apear one above each other
tbd: example

## Related components
tbd: example

## Change log
tbd: change log

## Feedback
Help us improve this pattern by providing feedback, asking questions, and leaving any other comments on [this form](https://forms.monday.com/forms/213ebddcb0d423ae5b6178fb6e8f7b3d?r=use1).