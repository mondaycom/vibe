import InfoIcon from "../InfoIcon";
import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { createStoryMetaSettingsDecorator } from "../../../storybook";
import { createComponentTemplate } from "vibe-storybook-components";
import { ICONS, TOOLTIP } from "../../../storybook/components/related-components/component-description-map";

export const metaSettings = createStoryMetaSettingsDecorator({
  component: InfoIcon
});

<Meta
  title="Feedback/InfoIcon"
  component={InfoIcon}
  argTypes={metaSettings.argTypes}
  decorators={metaSettings.decorators}
/>

export const infoIconTemplate = createComponentTemplate(InfoIcon);

# InfoIcon

- [Overview](#overview)
- [Props](#props)
- [Related components](#related-components)
- [Feedback](#feedback)

## Overview

InfoIcon is used to display information to the user when hovered. It is composed of <Link href="/?path=/docs/popover-tooltip--overview" withoutSpacing>Tooltip</Link> and <Link href="/?path=/docs/media-icon--overview" withoutSpacing>Icon</Link>.

<Canvas>
  <Story name="Overview" args={{ children: "Important information" }}>
    {infoIconTemplate.bind({})}
  </Story>
</Canvas>

## Props

<ArgsTable story="Overview" />

## Related components

<RelatedComponents componentsNames={[TOOLTIP, ICONS]} />
