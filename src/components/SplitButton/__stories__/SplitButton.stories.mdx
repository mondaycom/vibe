import SplitButton from "../SplitButton";
import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { createComponentTemplate } from "../../../storybook/functions/create-component-story";
import { Check, Search, Filter } from "../../Icon/Icons";
import Button from "../../Button/Button.jsx"
import "./splitButton.stories.scss";

<Meta
  title="Components/Split Button"
  component={ SplitButton }
/>

<!--- Component template -->

export const splitButtonTemplate = createComponentTemplate(SplitButton);
function SecondaryContentComponent() {
  return <div className="split-button-story-content">I can be whatever i want to be!</div>;
}

<!--- Component documentation -->

# Split Button
- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)
- [Do’s and don’ts](#dos-and-donts)
- [Use cases and examples](#use-cases-and-examples)
- [Related components](#related-components)
- [Feedback](#feedback)

## Overview
A split button is a dual-function menu button that offers a default action as well as the possibility of choosing a secondary action, by selecting from a set of alternatives.

<Canvas>
  <Story name="Overview"
         args={ {children: "Button"} }>
    { splitButtonTemplate.bind({}) }
  </Story>
</Canvas>

## Props
<ArgsTable of={ SplitButton } />

## Usage
<UsageGuidelines guidelines={[
  "A split button is a hybrid between a button and a menu: it groups related commands together into a dropdown, but also offers one-click access to a default choice that doesn’t require opening the menu.",
  "Limit the overall number of choices within the menu to less than four.",
  "Order the items within the menu by popularity for a small number of items, or alphabetically for a larger number of items.",
  "Avoid submenus within split button menu.",
  "Split button contains two actions: One primary action and a list of secondary actions."
]}/>

<Tip>If the actions in the menu are not related to each other, consider using a Menu component.</Tip>

## Variants
### Button types
The split button has three variants: primary, secondary, and tertiary.
<Canvas>
  <Story name="Button types">
    <SplitButton children="Primary" />
    <SplitButton children="Secondary" kind={SplitButton.kinds.SECONDARY} />
    <SplitButton children="Tertiary" kind={SplitButton.kinds.TERTIARY} />
  </Story>
</Canvas>

### Icons
<Canvas>
  <Story name="Icons">
    <SplitButton leftIcon={Check} children="Left icon" />
    <SplitButton rightIcon={Check} children="Right icon" />
    <SplitButton leftIcon={Check} />
  </Story>
</Canvas>

## Do’s and Don’ts
<ComponentRules
  rules={[
    {
      positive: {
        component:[<SplitButton children="New item" />,<SplitButton success kind={SplitButton.kinds.TERTIARY} leftIcon={Search}>Search</SplitButton>,<Button kind={Button.kinds.TERTIARY} leftIcon={Filter}>Filter</Button>    ],
        description:"Use only one primary action within a single view."
      },
      negative: {
        component:[<SplitButton children="New item" />,<Button>Search</Button>],
        description:"Don’t use multiple primary buttons within a single view."
      }
    },
    {
      positive: {
        component:<SplitButton children="New Item"  />,
        description:"Use split button if there are more than one action within the menu."
      },
      negative: {
        component:<SplitButton children="New Item" secondaryDialogContent={<><div>Oleg</div></>}/>,
        description:"Don’t use split button if there’s only one option within the menu. Use Button instead."
      }
    },
    {
      positive: {
        component:<SplitButton />,
        description:"Use a split button to display an action with related actions. The main action should be the most common."
      },
      negative: {
        component:<SplitButton />,
        description:"Don’t use a split button just to group actions."
      }
    }
  ]}
/>


## Use cases and examples
### Split button as the primary action
Use template is the main action.
<Canvas>
  <Story name="Split button as the primary action">
    <SplitButton />
    <SplitButton />
  </Story>
</Canvas>

### Seocndary split button 
When there’s already a primary button in the view, use a secondary split button.
<Canvas>
  <Story name="Seocndary split button">
    <SplitButton />
    <SplitButton />
  </Story>
</Canvas>

## Related components
<RelatedComponents componentsNames={[]} />