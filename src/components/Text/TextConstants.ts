export enum TextSize {
  MEDIUM = "medium",
  SMALL = "small"
}

export enum TextWeight {
  BOLD = "bold",
  NORMAL = "normal"
}

export enum TextColor {
  PRIMARY = "primary",
  SECONDARY = "secondary"
}
